# ── PostgreSQL ──
POSTGRES_USER=cryptohub
POSTGRES_PASSWORD=changeme
POSTGRES_DB=cryptohub

# ── Redis ──
REDIS_PASSWORD=changeme

# ── InfluxDB ──
INFLUX_USER=cryptohub
INFLUX_PASSWORD=changeme123
INFLUX_ORG=cryptohub
INFLUX_BUCKET=market_data
INFLUX_TOKEN=cryptohub-influx-token

# ── Kafka ──
KAFKA_BOOTSTRAP=kafka:9092

# ── JWT / Auth ──
SECRET_KEY=dev-secret-key-change-in-production

# ── Exchange API Keys (per-user, stored encrypted in DB) ──
# BINANCE_API_KEY=
# BINANCE_API_SECRET=
# OKX_API_KEY=
# OKX_API_SECRET=
# OKX_PASSPHRASE=
